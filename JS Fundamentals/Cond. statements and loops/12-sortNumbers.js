function descendSorting(a, b, c) {

    let array = [a, b, c]
    let separated = array.sort().reverse('')

    console.log(separated[0]);
    console.log(separated[1]);
    console.log(separated[2]);
}