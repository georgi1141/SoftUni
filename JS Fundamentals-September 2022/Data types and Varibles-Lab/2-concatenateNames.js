function concatenation(main, second, delimiter) {

    console.log(`${main}${delimiter}${second}`)

}

concatenation('John', 'Smith', '->')

// 2. Concatenate Names
// Write a function, which receives two names as string parameters and a delimiter.Print the names joined by the delimiter.